const showSideBar = () => {
    const sideBar = document.querySelector('.sidebar')
    sideBar.style.display = 'flex'
}

const hideSideBar = () => {
    const sideBar = document.querySelector('.sidebar')
    sideBar.style.display = 'none'
}